---
description: Markdown 中代码块嵌套时的围栏约定
globs: "**/*.md"
alwaysApply: false
---

# Markdown 代码嵌套

在 Markdown 里展示「代码中再包含代码块」时，内层的 ` ``` ` 会提前结束外层围栏，导致解析错乱。

**约定**：

- 外层代码块使用**更多反引号**作为围栏（例如 4 个：` ```` `）。
- 内层代码块继续使用 3 个反引号（` ``` `）。
- 这样内层的 ` ``` ` 不会匹配外层的 ` ```` `，整段能正确显示。

**示例**：用 ` ````markdown` 与 ` ```` ` 包裹整段含内层代码块的内容，内层仍用 ` ``` ` 或 ` ```lang`。
